configuration:
  appenders:
    console:
      name: CONSOLE
      target: SYSTEM_OUT
      encoder:
        pattern: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
  root:
    level: INFO
    appender-ref:
      - ref: CONSOLE